import {CHANNEL,redisPubSub} from '../../redisPubSub'
export const addMessage = (root, {message}) => {
    // console.log("mutation..");
    // const newMessage = `{ "icao24": "aa56da", "callsign": "UAL500"}}`;
    // redisPubSub.publish(CHANNEL, {messageAdded: newMessage});
    // return newMessage;
}